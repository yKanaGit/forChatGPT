hello world

# pip コマンドが見つからない場合は Python 経由でアップグレード
python -m pip install --upgrade pip

# 必要ライブラリのインストール
python -m pip install langchain langchain_community langchain_huggingface transformers accelerate torch

# (Hugging Face CLI でモデルを手動DLしたい場合)
python -m pip install huggingface_hub

# スクリプトを実行
python main.py